import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.adnanahmad.mymobileapp',
  appName: 'my-mobile-app',
  webDir: 'dist/my-mobile-app/browser'
};

export default config;
